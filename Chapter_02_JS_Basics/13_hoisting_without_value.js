console.log(a); //Output: ReferenceError: a is not defined
// hoisting will come after the declaration of the variable, 
// but before the initialization of the variable. So, in this case, a is not defined because it is not declared yet.
//In this case the variable a is not declared yet, so it will throw a ReferenceError.
//var a=10; //Declaration and Initialization